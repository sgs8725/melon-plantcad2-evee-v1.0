# =====================================================================
# Melon-PlantCAD2-EVEE 依赖
# PlantCAD2 = PlantCaduceus（Caduceus + Mamba2，双向 / 反向互补等变 / MLM）。
# 真实模型通过 trust_remote_code 加载，需要 mamba-ssm + causal-conv1d 与
# CUDA 内核（无法在纯 CPU 跑通）。仓库另附纯 PyTorch mock 编码器，可在 CPU
# 上跑通 scripts/smoke_test.py，无需安装 mamba-ssm。
# =====================================================================

# --- 核心深度学习 ---
torch>=2.1
# 注意：不要锁死在 transformers==4.44；PlantCAD2 的 trust_remote_code 建模代码
# 需要较新版本的 transformers 才能正确加载 Caduceus。
transformers>=4.44
accelerate>=0.30
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
pandas>=2.0
tqdm>=4.66
intervaltree>=3.1
einops>=0.7

# --- PlantCAD2 / Caduceus 真实推理所需（GPU + CUDA）---
# CPU 冒烟测试可跳过这两个；正式推理务必安装。
mamba-ssm>=2.2
causal-conv1d>=1.2

# --- EVEE 探针 / 数据 ---
safetensors>=0.4.0
polars>=1.0
pyarrow>=15.0

# --- 数据下载 / 基因组处理 ---
huggingface_hub>=0.23.0
pysam>=0.22.0
# 可选：读 bigWig 实验轨道（train_annotation_tracks.py 的 bigwig 类型需要；BED 类型无需）
pyBigWig>=0.3.18
# 可选：跨组装 liftOver（tracks_to_bed.py --liftover-chain 需要）
pyliftover>=0.4
biopython>=1.83
requests>=2.31.0

# --- 可选：LLM 机制解释合成 ---
anthropic>=0.40.0
